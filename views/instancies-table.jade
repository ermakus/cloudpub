- for item in items
    tr.service(class="srow-#{item.state}")
        td.service
            a.status(href="#", title="Instance ID: #{item.iid}")
                img(src="state-#{item.state}.png")
                .workers &nbsp;
            h3= item.iid
            
        td.domain
            h3= item.iid
        td.control
            - if(item.state == "down")
                img.btn.command(src="command-start.png",data-id=item.iid,data-command="start")
            - else 
                img.btn.command(src="command-stop.png",data-id=item.iid,data-command="stop")

